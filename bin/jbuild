(jbuild_version 1)

(executables (
  (names (main example))
  (libraries (async core httpaf-async sdn_local_protocol))
  (preprocess (pps (ppx_jane)))
  ))

(install
 ((section bin)
  (files ((main.exe as sdn-local)))))